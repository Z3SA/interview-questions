# Вопросы:
1. [Что такое HTML?](#1-что-такое-html)
2. [Что такое HTML-тег?](#2-что-такое-html-тег)
3. [Какая разница между HTML-элементом и HTML-тегом?](#3-какая-разница-между-html-элементом-и-html-тегом)
4. [Что нужно использовать для отображения данных в табличной форме в HTML?](#4-что-нужно-использовать-для-отображения-данных-в-табличной-форме-в-html)
5. [Что такое атрибут в HTML?](#5-что-такое-атрибут-в-html)
6. [Что такое "якорная ссылка" (или "ссылка-якорь")?](#6-что-такое-"якорная-ссылка"-или-"ссылка-якорь")
7. [Что такое списки в HTML?](#7-что-такое-списки-в-html)
8. [Что такое формы в HTML?](#8-что-такое-формы-в-html)
9. [Для чего комментарии в HTML?](#9-для-чего-комментарии-в-html)
10. [Что такое HTML5?](#10-что-такое-html5)
11. [Что такое семантичный или семантический HTML?](#11-что-такое-семантичный-или-семантический-html)
12. [Что такое Image Map?](#12-что-такое-image-map)
13. [Для чего тег `<embed>` в HTML?](#13-для-чего-тег-embed-в-html)
14. [Для чего тег `<marquee>` в HTML?](#14-для-чего-тег-marquee-в-html)
15. [Все ли HTML-теги имеют закрывающий тег?](#15-все-ли-html-теги-имеют-закрывающий-тег)
16. [Для чего нужен DOCTYPE?](#16-для-чего-нужен-doctype)
17. [Как следует оформлять страницу, содержимое которой может быть на разных языках?](#17-как-следует-оформлять-страницу-содержимое-которой-может-быть-на-разных-языках)
18. [На что необходимо обратить внимание при разработке мультиязычных сайтов?](#18-на-что-необходимо-обратить-внимание-при-разработке-мультиязычных-сайтов)
19. [Для чего нужны `data-` атрибуты?](#19-для-чего-нужны-data--атрибуты)
20. [Из каких блоков состоит HTML 5?](#20-из-каких-блоков-состоит-html-5)
21. [Разница между `<script>`, `<script async>`, `<script defer>`.](#21-разница-между-script-script-async-script-defer)
22. [Почему хорошей практикой считается располагать `<link>` для подключения CSS внутри `<head>`, а `<script>` для подключения JS ставить перед `</body>`? Есть ли исключения?](#22-почему-хорошей-практикой-считается-располагать-link-для-подключения-css-внутри-head-а-script-для-подключения-js-ставить-перед-body-есть-ли-исключения)
23. [Что такое прогрессивный рендеринг?](#23-что-такое-прогрессивный-рендеринг)
24. [Для чего используется атрибут srcset в теге изображения? Опишите процесс, который браузер использует при обработке содержимого этого атрибута.](#24-для-чего-используется-атрибут-srcset-в-теге-изображения-опишите-процесс-который-браузер-использует-при-обработке-содержимого-этого-атрибута)
25. [Какие есть языки HTML-шаблонизации?](#25-какие-есть-языки-html-шаблонизации)
26. [В чём преимущество схлопывания пустых пробелов?](#26-в-чём-преимущество-схлопывания-пустых-пробелов)
27. [Для чего атрибут `class`?](#27-для-чего-атрибут-class)
28. [В чём разница между `id` и `class` атрибутами?](#28-в-чём-разница-между-id-и-class-атрибутами)
29. [Какие существуют типы DOCTYPE?](#29-какие-существуют-типы-doctype)
30. [Как указать кодировку в документе?](#30-как-указать-кодировку-в-документе)

## 1. Что такое HTML?

HTML - аббревиатура от Hypertext Markup Language. Это язык разметки веб-документов для отображения в браузерах.

Стандартные расширения для HTML-кода - `.html` и `.htm`.

## 2. Что такое HTML-тег?

В HTML теге обозначается HTML-элемент, который, в свою очередь, даёт инструкцию, как браузеру отображать контент. Тег оборачивается в угловые скобки (`<` и `>`). Открывающий тег должен иметь "симметричный" закрывающий тег после него.

## 3. Какая разница между HTML-элементом и HTML-тегом?

HTML-элемент - часть веб-страницы. Это может быть параграф, изображение, ссылка или другой элемент, форматирующий контент.

HTML-тег - инструмент для обозначения положения и "границ" HTML-элемента (см. вопрос №2).

## 4. Что нужно использовать для отображения данных в табличной форме в HTML?

Тег `<table>` и его дочерние - `<tr>`, `<td>`, `<tbody>` и т.д.

## 5. Что такое атрибут в HTML?

Атрибут устанавливает дополнительное поведение HTML-элементу. Они указываются внутри тега (если есть закрывающий тег, то атрибуты указываются внутри открывающего). Пример - атрибут `type` для тега `<input>`.

## 6. Что такое "якорная ссылка" (или "ссылка-якорь")?

Якорная ссылка позщволяет перейти на определённую секцию внутри HTML-страницы. 

Пример кода: `<a href="#section">Перейти к секции</a>` или `<a href="#" target="section">Перейти к секции</a>`.

## 7. Что такое списки в HTML?

Списки в HTML позволяют показать, как бы не было неожиданно, связанные элементы в виде списка.

Для списков используются следующие теги:
- нумерованный список (`<ol>`);
- ненумерованный или маркированный список (`<ul>`);
- описывающий список (`<dl>`);
- навигационный список или меню (`<menu>`);
- список каталогов, директорий или папок (`<dir>`).

Элементы внутри списка обозначаются тегом `<li>`.

## 8. Что такое формы в HTML?

С помощью форм можно собирать данные от пользователя и отправлять их на бэкенд. Данные вводятся в элементы, обозначенные тегом `<input>`. Тег - `<form>`.

## 9. Для чего комментарии в HTML?

С помощью комментариев (`<!-- Комментарий -->`) можно оставлять важные заметки, указывать сложные участки кода или временно "отключать" ненужный код.

## 10. Что такое HTML5?

HTML5 - обновлённая версия HTML, выпущенная в 2014 году консорциумом World Wide Web (W3C). Помимо новых тегов новый стандарт добавил множество вещей, доступных через JavaScript, как стандарт в браузерах (`sessionStorage`, `localStorage`, Canvas, различные браузерные API и т.д.).

## 11. Что такое семантичный или семантический HTML?
 
Семантический HTML - стиль/стайлгайд оформления кода, в котором используемые теги полностью или частично описывают находящийся в них контент.

Примеры:
- `<header>` для обозначения шапки сайта;
- `<footer>` для обозначения подвала сайта;
- `<strong>` вместо `<b>` для обозначения жирного текста;
- `<p>` для обозначения параграфов или просто участков текста.

## 12. Что такое Image Map?

Image Map (`<map>`) используется с элементами `<area>` для определения карты изображений (интерактивной области ссылок).

## 13. Для чего тег `<embed>` в HTML?

Тег `<embed>` использовался для включения аудио или видео в HTML-документ. Сейчас лучше использовать для вставки интерактивных аудио или видео теги `<audio>` и `<video>` соответственно.

## 14. Для чего тег `<marquee>` в HTML?

`<marquee>` используется для создания бегущей строки по вертикали или горизонтали. "Бегущим" может быть не только текст, но и другие HTML-элементы. Использование этого тега невалидно с точки зрения спецификации HTML 4.01, да и использовать бегущую строку - моветон.

## 15. Все ли HTML-теги имеют закрывающий тег?

Нет. Существуют некоторые теги, которые не могут иметь внутри контента, они закрываются сразу же. Примеры: `<img />` или `<br />`. Такие теги необязательно обозначать "самозакрывающимися" с точки зрения HTML 4.01 или HTML 5, но XHTML или JSX такой открытый тег не примет.

## 16. Для чего нужен DOCTYPE?

DOCTYPE - сокращение от Document Type. DOCTYPE всегда связан с DTD - Document Type Definition.

DTD определяет определяет, как должны быть структурированы документы определённого типа. DOCTYPE объявляет, к какому DTD предположительно относится документ.

DOCTYPE определяет, какая спецификация HTML используется в текущем документе. Как только обнаруживается DOCTYPEЮ, браузер запускает режим `no-quirks`, соответствующий этому DOCTYPE, иначе активируется режим `quirks`.

DOCTYPE стандарта HTML 5 - `<!DOCTYPE html>`.

## 17. Как следует оформлять страницу, содержимое которой может быть на разных языках?

Предполагается, что речь о наиболее частом случае: как показывать страницу, где содержимое доступно на нескольких языках, но отображается на одном определенном.

Когда к серверу делается HTTP-запрос, браузер отсылает информацию о предпочитаемом языке в хедере `Accept-Language`. Это может использовать сервер. В возвращённом HTML должен быть указан корректный язык в атрибуте `lang` тега `<html>`. 

Ну а дальше поддержка переводов любыми доступными решениями, которые используют разработчики продукта.

## 18. На что необходимо обратить внимание при разработке мультиязычных сайтов?

1. Использование атрибута `lang` в HTML.
2. Перенаправление пользователя на версию сайта с языком, понятным пользователю. При этом должна быть возможность быстро сменить язык и страну в интерфейсе.
3. Текст на картинках плохо поддаётся адаптации, поэтому весь текст по максимуму должен быть в коде.
4. Необходимо учитывать разную длину строк на разных языках в интерфейсе.
5. Форматирование дат и времени под стандарт, принятый в стране пользователя.
6. Не склеивать переводимые строки, вместо склеивания нужно использовать шаблоны.
7. Разные направления чтения.

## 19. Для чего нужны `data-` атрибуты?

https://www.frontendinterviewhandbook.com/ru/html-questions/#%D0%B4%D0%BB%D1%8F-%D1%87%D0%B5%D0%B3%D0%BE-%D0%BD%D1%83%D0%B6%D0%BD%D1%8B-data--%D0%B0%D1%82%D1%80%D0%B8%D0%B1%D1%83%D1%82%D1%8B

## 20. Из каких блоков состоит HTML 5?

1. Семантика.
2. Связанность (общение с серверами новыми методами).
3. Офлайн и хранилище.
4. Мультимедиа.
5. 2D, 3D и эффекты.
6. Производительность и интеграции.
7. Доступ к разным устройствам.
8. Стилизация.

## 21. Разница между `<script>`, `<script async>`, `<script defer>`.

- `<script>` - отрисовка HTML блокируется, выпонляется скрипт, отрисовка HTML продолжается.
- `<script async>` - скрипт будет получен и обработан параллельно с отрисовкой HTML. Его лучше использовать, когда скрипт внутри не зависит от других (к примеру, аналитика).
- `<script defer>` - скрипт будет получен параллельно с отрисовкой, но выполняться будет только после полной загрузки страницы. Если таких скриптов несколько, они будут выполняться по порядку объявления.

Атрибуты `defer` и `async` игнорируются, если у тега нет атрибута `src`.

## 22. Почему хорошей практикой считается располагать `<link>` для подключения CSS внутри `<head>`, а `<script>` для подключения JS ставить перед `</body>`? Есть ли исключения?

### Размещение `<link>` внутри `<head>`

Размещение `<link>` внутри тега `<head>` необходимо при создании оптимизированного веб-сайта. Когда страница загружается, HTML и CSS анализируются одновременно, то есть HTML создаёт DOM, а CSS - CSSOM. И то, и другое необходимо для создания визуальных элементов на сайт, что позволяет быстро и точно определить время первой значимой отрисовки. Этот прогрессивный рендеринг является частью оптимизации сайтов и поддаётся аналитике. Размещение таблиц стилей в нижней части сайта препятствует прогрессивному рендерингу во многих браузерах, в итоге пользователь может долго видеть неоформленные элементы сайта.

### Размещение `<script>` перед `</body>`

Теги `<script>` блокируют отрисовку HTML на то время, пока они скачиваются и исполняются. Размещение скриптов внизу позволяет сперва распарсить и показать пользователю весь HTML.

Исключение - использование `defer` или `async`.

## 23. Что такое прогрессивный рендеринг?

Это технология ускорения отрисовки страниц, чтобы показать пользователю контент как можно скорее.

Примеры решения прогрессивного рендеринга:
- ленивая загрузка изображений;
- приоритизация видимого контента (грузить код только для того, что будет видно пользователя в момент времени);
- асинхронные фрагменты HTML.

## 24. Для чего используется атрибут srcset в теге изображения? Опишите процесс, который браузер использует при обработке содержимого этого атрибута.

https://www.frontendinterviewhandbook.com/ru/html-questions/#%D0%B4%D0%BB%D1%8F-%D1%87%D0%B5%D0%B3%D0%BE-%D0%B8%D1%81%D0%BF%D0%BE%D0%BB%D1%8C%D0%B7%D1%83%D0%B5%D1%82%D1%81%D1%8F-%D0%B0%D1%82%D1%80%D0%B8%D0%B1%D1%83%D1%82-srcset-%D0%B2-%D1%82%D0%B5%D0%B3%D0%B5-%D0%B8%D0%B7%D0%BE%D0%B1%D1%80%D0%B0%D0%B6%D0%B5%D0%BD%D0%B8%D1%8F-%D0%BE%D0%BF%D0%B8%D1%88%D0%B8%D1%82%D0%B5-%D0%BF%D1%80%D0%BE%D1%86%D0%B5%D1%81%D1%81-%D0%BA%D0%BE%D1%82%D0%BE%D1%80%D1%8B%D0%B9-%D0%B1%D1%80%D0%B0%D1%83%D0%B7%D0%B5%D1%80-%D0%B8%D1%81%D0%BF%D0%BE%D0%BB%D1%8C%D0%B7%D1%83%D0%B5%D1%82-%D0%BF%D1%80%D0%B8-%D0%BE%D0%B1%D1%80%D0%B0%D0%B1%D0%BE%D1%82%D0%BA%D0%B5-%D1%81%D0%BE%D0%B4%D0%B5%D1%80%D0%B6%D0%B8%D0%BC%D0%BE%D0%B3%D0%BE-%D1%8D%D1%82%D0%BE%D0%B3%D0%BE-%D0%B0%D1%82%D1%80%D0%B8%D0%B1%D1%83%D1%82%D0%B0

## 25. Какие есть языки HTML-шаблонизации?

Я только Pug знаю.

## 26. В чём преимущество схлопывания пустых пробелов?

Это помогает разработчикам писать табуляции сколько угодно и не париться, что они будут отображаться в браузере таким же количеством пробелов. Вместо них браузер будет отображать один пробел.

## 27. Для чего атрибут `class`?

Для CSS классов. Можно писать туда их сколько угодно.

## 28. В чём разница между `id` и `class` атрибутами?

Атрибут `id` нужен для указания идентификатора элемента. Он может быть только один, уникальный на весь документ (XHTML на это ругается). Используется для JS.

Про атрибут `class` выше.

## 29. Какие существуют типы DOCTYPE?

- Strict doctype (XHTML?)
- Transitional doctype (дефолт)
- Frameset doctype

## 30. Как указать кодировку в документе?

`<meta charset="utf-8" />` в `<head>`.
## Источники

- [Frontend Interview Handbook](https://www.frontendinterviewhandbook.com/ru/html-questions/)
- [Upgrad | 15 HTML Interview Questions](https://www.upgrad.com/blog/html-interview-questions-answers/)
- [InterviewBit](https://www.interviewbit.com/html-interview-questions/)